[package]
name = "sln_parser"
version = "0.1.0"
edition = "2021"

[dependencies]
uuid = { version = "1.2", features = ["serde"] }
regex = "1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.0", features = ["derive"] }
